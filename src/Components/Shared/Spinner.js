import React from 'react';

const Spinner = () => {
    return (
        <div className="w-full h-screen flex justify-center items-center">
            <button class="btn loading text-center text-black bg-green-600 border-none">loading</button>
        </div>
    );
};

export default Spinner;